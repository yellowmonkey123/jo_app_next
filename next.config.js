// next.config.js

/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true, // Example: keep default strict mode
  // Add any other Next.js config options you need here
  // e.g., images: { domains: [...] },
};

module.exports = nextConfig;